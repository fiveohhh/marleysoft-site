---
import Layout from '../layouts/Layout.astro';
---

<Layout
  title="Contact - Marleysoft"
  description="Get in touch to discuss your embedded firmware project. Let's talk about architecture, testing infrastructure, and building sustainable firmware systems."
>
  <!-- Header -->
  <section class="bg-brand-dark text-white py-12 sm:py-16">
    <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
      <h1 class="text-3xl sm:text-4xl lg:text-5xl font-bold mb-3 sm:mb-4">Get in Touch</h1>
      <p class="text-lg sm:text-xl text-gray-200 max-w-3xl">
        Let's discuss how I can help your team build better firmware systems.
      </p>
    </div>
  </section>

  <!-- Contact Form Section -->
  <section class="py-12 sm:py-16 bg-white">
    <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">

      <div class="mb-8 sm:mb-12">
        <p class="text-base sm:text-lg text-gray-700 mb-3 sm:mb-4">
          Whether you're starting a new project, dealing with technical debt, or looking to improve your development infrastructure, I'd be happy to discuss how I can help.
        </p>
        <p class="text-base sm:text-lg text-gray-700">
          Fill out the form below and I'll get back to you within 24 hours.
        </p>
      </div>

      <!-- Netlify Form -->
      <form
        name="contact"
        method="POST"
        data-netlify="true"
        netlify-honeypot="bot-field"
        class="space-y-6"
      >
        <!-- Hidden fields for Netlify -->
        <input type="hidden" name="form-name" value="contact" />
        <p class="hidden">
          <label>
            Don't fill this out if you're human: <input name="bot-field" />
          </label>
        </p>

        <!-- Name -->
        <div>
          <label for="name" class="block text-sm font-medium text-gray-700 mb-2">
            Name *
          </label>
          <input
            type="text"
            id="name"
            name="name"
            required
            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-brand-copper focus:border-transparent"
          />
        </div>

        <!-- Email -->
        <div>
          <label for="email" class="block text-sm font-medium text-gray-700 mb-2">
            Email *
          </label>
          <input
            type="email"
            id="email"
            name="email"
            required
            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-brand-copper focus:border-transparent"
          />
        </div>

        <!-- Company -->
        <div>
          <label for="company" class="block text-sm font-medium text-gray-700 mb-2">
            Company (Optional)
          </label>
          <input
            type="text"
            id="company"
            name="company"
            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-brand-copper focus:border-transparent"
          />
        </div>

        <!-- Message -->
        <div>
          <label for="message" class="block text-sm font-medium text-gray-700 mb-2">
            Message *
          </label>
          <textarea
            id="message"
            name="message"
            required
            rows="6"
            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-brand-copper focus:border-transparent"
            placeholder="Tell me about your project or what challenges you're facing..."
          ></textarea>
        </div>

        <!-- Submit Button -->
        <div>
          <button
            type="submit"
            class="w-full bg-brand-copper text-white px-6 sm:px-8 py-3 rounded-lg font-semibold hover:bg-brand-copper-light transition-colors min-h-[48px]"
          >
            Send Message
          </button>
        </div>
      </form>

    </div>
  </section>

  <!-- Additional Info Section -->
  <section class="bg-brand-dark-50 py-12 sm:py-16">
    <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">
      <h2 class="text-xl sm:text-2xl font-bold mb-4 sm:mb-6 text-center">What to Expect</h2>
      <div class="space-y-3 sm:space-y-4">
        <div class="flex items-start">
          <svg class="w-5 h-5 sm:w-6 sm:h-6 text-brand-copper mr-2 sm:mr-3 mt-1 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
          <p class="text-sm sm:text-base text-gray-700"><strong>Quick Response</strong> — I'll get back to you within 24 hours</p>
        </div>
        <div class="flex items-start">
          <svg class="w-5 h-5 sm:w-6 sm:h-6 text-brand-copper mr-2 sm:mr-3 mt-1 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z" />
          </svg>
          <p class="text-sm sm:text-base text-gray-700"><strong>Initial Conversation</strong> — We'll discuss your needs and see if I'm a good fit</p>
        </div>
        <div class="flex items-start">
          <svg class="w-5 h-5 sm:w-6 sm:h-6 text-brand-copper mr-2 sm:mr-3 mt-1 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
          <p class="text-sm sm:text-base text-gray-700"><strong>No Pressure</strong> — If it's not a good fit, I'll be honest about it</p>
        </div>
      </div>
    </div>
  </section>
</Layout>
